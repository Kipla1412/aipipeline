# Medical Knowledge Platform — Python dependencies
# Preferred install: uv sync  (uses pyproject.toml + uv.lock)
# Alternative:        pip install -r requirements.txt
#
# Note: `filenest` is an internal SDK, not on PyPI.
# Install it from the local checkout:
#   uv add filenest-python-sdk --source /home/kipla/filenest-python-sdk

# ── Core ──
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
pandas

# ── LLM ──
openai>=2.0
httpx>=0.27

# ── Document extraction ──
pymupdf>=1.24
pydicom>=3.0
numpy>=1.26
pillow>=10.0
psycopg2-binary
# ── Graph databases ──
python-arango>=8.0
neo4j>=5.0

# ── FHIR ──
fhir.resources>=8.0

# ── Testing ──
pytest>=8.0
pytest-asyncio>=0.23

# ── FileNest internal SDK (local editable install) ──
# -e /home/kipla/filenest-python-sdk

# ============================================================
# Airflow (separate venv — /home/kipla/airflow/venv)
# ============================================================
# Airflow runs from its OWN venv, NOT this project's .venv.
# Install into the Airflow venv directly:
#
#   /home/airflow/venv/bin/pip install apache-airflow==3.1.5
#   /home/airflow/venv/bin/pip install "apache-airflow-providers-standard"
#   /home/airflow/venv/bin/pip install "apache-airflow-providers-postgres"
#   /home/airflow/venv/bin/pip install -e /home/kipla/filenest-python-sdk
#   /home/airflow/venv/bin/pip install psycopg2-binary sqlalchemy pydantic python-dotenv
#
# The DAGs load credentials from this project's .env (via python-dotenv),
# so no Airflow Connections are required for FileNest/Postgres.
# ============================================================

