- Expects pipeline processing to be idempotent — re-running the pipeline should skip already-processed files, not reprocess them. Explicitly confirmed with "yeah" when asked whether to add dedup (a `processed` status so the producer skips files already downloaded/processed and the consumer only transforms unprocessed records). Confidence: 0.95
- When the user asks how to inspect/read a database ("can u explain how to read the db da"), expects a practical, tutorial-style answer with copy-pasteable commands rather than a conceptual overview: direct psql access using the credentials from `.env` (e.g., `PGPASSWORD=... psql -h localhost -U postgres -d fhir-staging`), the API/curl alternative for the same data, an explanation of the relevant tables and how they relate (e.g., staging_record → observation), and a set of useful read-only queries (status counts, stuck `processing` records, observations per record). Teaches them to query the data themselves, and clarifies which data is local vs. which lives on a colleague's remote database. When they then ask "where i write the code for this da", they want a committed, runnable script in the repo, not just chat commands — the assistant delivered a read-only DB viewer at `tests/manual/read_staging_db.py` (house-format docstrings, credentials loaded from `.env` via dotenv, parameterized SQLAlchemy `text()` queries, human-readable table plus status counts, optional `<record_id>` arg to print that record's observations) placed alongside the other manual scripts. Confidence: 0.66
- Wants Python project dependencies tracked in a `requirements.txt` at the project root — explicitly requests the file be added (e.g., "can u add the requirement.txt file please") so installs are reproducible, including documented handling of internal/vendor SDKs not on public PyPI. Confidence: 0.72
- Wants `requirements.txt` to be comprehensive, not just headline dependencies — explicitly asked to "add the basic requirements also" after an initial version; expects the file to capture every package the codebase actually imports (derived by scanning src/, dags/, and scripts), organized in purpose-grouped sections with inline comments (e.g., pyyaml, pypdfium2, sqlalchemy, pandas, redis, boto3, elasticsearch, opensearch-py, plus commented-out optional deps like apache-airflow). Confidence: 0.70
- Wants dependency documentation to state basic environment prerequisites alongside the pip packages — explicitly asks for "basic things we need python3.12 like that", i.e., the required Python version (3.12) and similar runtime baseline should be documented in the requirements/setup docs so contributors know the interpreter version, not just the package list. Confidence: 0.70
- Wants `uv` as the dependency management tool for the pipeline — explicitly requested ("i want to use uv in this pipeline da can u add this"); expects a `pyproject.toml` (project metadata, `[project]` dependencies derived from the existing requirements.txt, dev dependency-groups, pytest config) plus a generated `uv.lock`, superseding the plain requirements.txt-only workflow. Internal/vendor SDKs not on PyPI (e.g., `filenest-python-sdk`) must be declared as a direct dependency with a `[tool.uv.sources]` path entry pointing at the local checkout so they land in the lockfile. Confidence: 0.85
- Prefers generalizable matching logic (e.g., `.+` to capture full filenames including spaces, rather than `\S+` which breaks on multi-word names) over hardcoded assumptions (e.g., `.pdf` regex that excludes `.dcm`). Confidence: 0.85
- Expects standalone diagnostic scripts alongside platform components to verify infrastructure connectivity before running full pipelines — e.g., a `check_arango.py` script that inspects collections and confirms data was written. Confidence: 0.80
- When asked how to verify that an integration is working (e.g., "how i say the staging is working or not can u understand what i say"), expects a single runnable self-check script (e.g., `check_staging.py` at project root) that prints a clear pass/fail verdict (e.g., "✓ fhir-staging bridge is WORKING") and tells the user the exact command to run — the check must cover config reading the base URL from `.env`, service health, client/mapper construction, a real write round-trip against the external service (create a test record → PATCH it back → status completed), and a read-back of real records. Confidence: 0.70
- Expects end-to-end pipeline verification scripts at project root (not in `tests/`) that run real PDF/DICOM files through the full extract→classify→transform chain and print structured output plus consumer compatibility checks — e.g., `test_transformer.py` invoked as `python3 test_transformer.py file.pdf`. Distinct from mock-based pytest unit tests and distinct from infrastructure connectivity checks. Confidence: 0.82
- Prefers concrete data examples and actual database content dumps over abstract architecture diagrams when verifying that infrastructure is working — wants to see real rows/nodes/edges, not just descriptions of them. Confidence: 0.80
- Verification/flow scripts that print human-readable summaries of the extracted/transformed result should also dump the raw JSON output — wants the actual serialized data (e.g., the transformed Clinical Domain Model as JSON) visible/inspectable, not just formatted summary lines. Confidence: 0.70
- Wants end-to-end verification to produce the actual concrete artifact as proof of a change — when confirming a field flows through a mapping, the assistant ran the full chain (transformer observation → `StagingObservationMapper` → the exact PATCH payload JSON) and printed the resulting payload so the user could see exactly where the value lands; a real runnable demonstration of the output beats a description of the flow. Confidence: 0.55
- Reports issues (and shares output) by pasting raw terminal/API output verbatim rather than describing it — tracebacks, logs, and large data dumps alike (e.g., a garbled/truncated FHIR JSON blob with mostly-null fields pasted as the entire message: "i see null ..."). May or may not include an explicit question like "can u understand" or "what i do"; often the raw output is the whole message with zero commentary, and expects the assistant to interpret it and show a clean human-readable view. Confidence: 0.99
- Shares logs from a colleague's machine/environment (e.g., a full Airflow task log with paths under the colleague's checkout like `/home/gnr/work/code/nisha/aipipeline/...`) and expects cross-environment forensics: read the colleague's log as ground truth for what happened on their run, distinguish that environment's state (their DB, their project path) from the local one, and diagnose the shared code paths (e.g., "the consumer ran fine on their machine; the real failures are producer-side, already visible in the local producer log") rather than treating the pasted log as a local artifact. Confidence: 0.60
- When an error occurs, asks why ("y this errors or occur" / "y this errors occur") and expects a plain-language root-cause explanation — the import/dependency chain that produced the failure (e.g., eager `__init__.py` imports forcing optional SDKs like `python-arango` to be installed) — not just a silent fix; only after understanding the cause does the user green-light the remedy ("okay install it"). Confidence: 0.65
- Rejects mock/simulated test cases that embed fake data (e.g., hardcoded `llm_output` dicts, `AsyncMock` fixtures) — wants verification scripts that process real PDF/DICOM files through the actual extraction and transformation pipeline with live LLM calls, producing real JSON output from real documents. Explicitly requests a "real flow test case" when only mock-based unit tests have been delivered so far. Confidence: 0.90
- Proactively asks "what about X?" to identify gaps or missing features in an implementation, expecting either justification for the omission or a plan to add the missing piece. Confidence: 0.78
- Configures infrastructure via `.env` files and expects connection config to live in the central `credentials/local/` module with env-driven values and local defaults. Refined by later evidence: credentials are consumed through the existing provider utility (`LocalCredentialProvider`), not via newly-invented helper functions in `config.py` — a `get_rdbms_config()` helper added there was explicitly rejected and reverted (see LocalCredentialProvider bullet). Confidence: 0.60
- Expects connection configuration for each service/database to support a host-URL form (e.g., FileNest `base_url`, "postgres host url") mirroring the pattern used by existing connectors, not only separate host/port fields — refers to "host url" when asking for connection config to be added. Confidence: 0.60
- Values SOLID principles and Clean Architecture in software design — especially Single Responsibility, Open/Closed, and Dependency Inversion. Splits monolithic classes into focused concerns rather than allowing multi-responsibility classes to persist. Confidence: 0.95
- Prefers architectural review and migration plan before any code changes for refactoring tasks — explicitly requires reviewing existing code, identifying responsibilities to move, and explaining the migration strategy before implementation begins. Confidence: 0.90
- Treats backward compatibility as a hard constraint during refactoring — outputs must remain identical, downstream components must not require modification, and the refactoring must be additive/reversible. Confidence: 0.95
- Prefers strict separation of infrastructure concerns from business logic — connectors validate config, manage connections, and expose clients, but must NEVER contain domain logic, entity insertion, or repository behavior. Confidence: 0.90
- Prefers dependency injection for wiring repositories to connectors — repositories receive pre-configured connectors rather than creating their own connections. Confidence: 0.85
- Uses cloud-managed infrastructure (ArangoDB Oasis) rather than self-hosted database instances. Cloud connectors must support TLS, HTTPS, and standard cloud auth (username/password). Confidence: 0.85
- Uses a hybrid infrastructure model — cloud services (ArangoDB Oasis) alongside local Docker containers (Neo4j via `bolt://localhost:7687`). Pragmatic about deployment strategy: picks the hosting model that fits each service rather than standardizing on one. Confidence: 0.70
- Asks for rationale behind technology and protocol choices — e.g., "why we use bolt" when encountering an unfamiliar protocol; also challenges field-mapping decisions into external APIs ("y u mapped into note" — why map `ai_summary` to FHIR's `note` field?) and expects the justification grounded in the standard/API schema (FHIR R4 `note` is the native field for clinical comments; the staging API's ObservationInput schema has no `summary`/`ai_summary` field), with readiness to switch the field name if the real schema uses a different one. Fresh evidence: when told the standards-based rationale, the user pushed back ("i think this is not good da") and the accepted resolution was empirical — fetching the live API's `openapi.json` and confirming `note: [ObservationNoteInput]` is a real, validated field in the schema, rather than re-arguing from the FHIR standard. Wants mappings verified against the actual API contract, not just architectural reasoning. Confidence: 0.75
- Expects data retrieval and graph traversal test scripts alongside infrastructure check scripts — e.g., `query_arango.py` that runs AQL graph traversals and prints patient→disease→doctor summaries, not just connection verification. Confidence: 0.75
- Prefers defensive normalization of user-provided configuration values — connectors should strip protocol prefixes, trailing ports, and other artifacts from config inputs (e.g., hostnames), and also prepend a missing scheme when users enter bare hostnames (e.g., an Airflow connection Host of `filenest.drgodly.com` without `https://` produced `UnsupportedProtocol: Request URL is missing an 'http://' or 'https://' protocol` at runtime) so users can paste/enter URLs without manual cleaning. Normalization must also handle hosts that ALREADY include the scheme: blindly prepending `https://` to a connection Host of `https://api.jina.ai` produced `https://https://api.jina.ai/v1/`, which made httpx resolve the bogus hostname `https` → `Temporary failure in name resolution` in the Jina embedder's retry loop. Check `startswith(('http://','https://'))` before prepending, and strip trailing slashes. Fresh evidence: a connection's `base_url` may already include the full endpoint path — the jina_api Extra the user pasted had `base_url: "https://api.jina.ai/v1/embeddings"` — which must be normalized to the connector's base (`https://api.jina.ai/v1/`) by stripping the trailing `/embeddings`; otherwise the client appends its own relative `"embeddings"` path and every request hits `.../embeddings/embeddings` (double path). The user pastes the exact Airflow connection Extra JSON (secret masked as `***`) as the authoritative ground-truth shape the code must accommodate. Confidence: 0.85
- When adding a new backend or integration, mirrors the existing reference implementation's architecture exactly — same file structure, same connector/repository patterns, same abstractions. Never invents a new pattern when a working one already exists in the codebase. Fresh evidence: the indexing spec repeatedly demanded reuse of the existing Jina embedder and OpenSearch connector ("I already have a Jina embedding connector/provider. Reuse it. DO NOT create another Jina client. / I already have an OpenSearch connector. Reuse it. DO NOT create another OpenSearch client."), reuse of the existing factories/connectors/schemas/config, and an embedding-provider abstraction that lets Jina be swapped for Google Embedding later without touching the chunker ("The chunker should remain independent of Jina"). The assistant followed by building on the actual `JinaEmbeddingsService(connection, config)` interface. Confidence: 0.93
- Prefers `logging` over `print()` — production code should use meaningful logging with appropriate log levels. Print statements are tolerated only in top-level pipeline orchestration scripts for user-facing status output. Fresh evidence: the indexing-layer spec explicitly demanded useful logs for files indexed, chunk/embedding/index counts, duration and failures — while explicitly forbidding logging API keys, passwords, full medical document contents, and unnecessary PHI ("Do NOT log: API keys, passwords, complete medical document contents, unnecessary PHI") — and forbade silently swallowing exceptions, requiring failures to be handled clearly between chunking, embedding, and indexing stages. Confidence: 0.92
- Expects the assistant to explain the architectural approach, identify which files will change with rationale, and describe integration strategy before generating any code. Jumping straight to implementation without architectural framing is dispreferred. Fresh evidence: the indexing spec explicitly ordered "First inspect the codebase and understand the existing interfaces before coding" and mandated the exact field names/interfaces be understood before writing ("Do NOT invent fields that are not present in the project"); the assistant's first moves were read-only inspection greps and reads of every relevant interface before any edit. Confidence: 0.90
- Prefers parameterized database queries — never concatenates SQL/Cypher/AQL strings manually. Always uses bind variables / query parameters to prevent injection and enable query plan caching. Confidence: 0.85
- Error handling: exceptions should be typed by architectural layer — infrastructure failures raise infrastructure exceptions, persistence failures raise repository exceptions. Never silently swallows exceptions; always includes useful diagnostic context in the log. Confidence: 0.80
- Performance-conscious: reuses database connections (does not open a new connection per operation), batches writes where possible, avoids N+1 query patterns, and avoids unnecessary graph traversals. Confidence: 0.75
- Uses strict typing (Python 3.12+ type hints) — all function signatures, method parameters, and return types should be fully annotated. Pydantic models for configuration and data transfer. Confidence: 0.80
- Prefers small, focused methods with single responsibilities. Avoids large monolithic functions. Prefers composition over inheritance when designing class hierarchies. Confidence: 0.78
- Centralizes graph entity definitions in a single builder — when adding a new entity type (e.g., Report nodes), expects it added once to `MedicalGraphBuilder`, with all repository backends (Neo4j, ArangoDB, JSON) updated through their type-label/collection maps rather than patching each repository independently. Confidence: 0.80
- Uses a serializer-boundary pattern for backward compatibility — when upgrading internal domain models to be more structured (e.g., Pydantic `Diagnosis` instead of `List[str]`), a serializer layer at the component boundary converts rich internal objects back to the original flat format so downstream consumers see zero difference. Rich internally, flat externally. Confidence: 0.82
- Every clinical concept requires its own strongly-typed Pydantic model — `Diagnosis`, `Medication`, `Procedure`, `Observation` each get dedicated model classes with typed fields. Flat `List[str]` representations are legacy that should be upgraded. Confidence: 0.85
- All measurable clinical findings — vitals, labs, ECG measurements, imaging metrics, pathology — belong in a single unified `Observation` model rather than siloed category-specific models. No separate "Vitals" model distinct from other observations. Confidence: 0.82
- The clinical domain model must be completely independent of downstream data standards (FHIR, HL7, SNOMED, LOINC). Standard mapping happens in a dedicated layer outside the transformer — the transformer knows only clinical concepts, never FHIR resources. The transformer must also never generate markdown, wiki content, perform terminology mapping, or communicate with FHIR servers. Confidence: 0.90
- Prefers a Builder → Normalizer → Validator processing pipeline architecture with dedicated layer folders (`builders/`, `normalizers/`, `validators/`) and ABC interfaces in `interfaces/`. Each layer has exactly one responsibility: construction, standardization, and constraint checking respectively. The transformer composes these layers rather than embedding their logic. Confidence: 0.88
- Dislikes `__init__.py` files that contain implementation code — `__init__.py` should be a thin re-export layer only. Actual ABCs, classes, and logic belong in named modules (e.g., `base.py`), matching the project convention already used in `connectors/base.py` and `graphbuilder/repository/base.py`. Confidence: 0.85
- Blood Pressure must be modeled as a decomposable structured type (systolic/diastolic as floats), never a plain string like "138/84". The model must support future decomposition without breaking existing APIs. Confidence: 0.90
- Numeric clinical observation values must use Python numeric types (int/float), never be stored as strings. E.g., Hemoglobin = 14.2 not "14.2". Confidence: 0.92
- Observation display names must be normalized to canonical forms to prevent duplicates — e.g., "Body Temperature" → "Temperature", "Body Weight" → "Weight", "Body Height" → "Height". Normalization happens before the MedicalSchema is returned. Confidence: 0.88
- Unit values must be sanitized: never duplicated ("kg kg" → "kg", "cm cm" → "cm"), never the string "None" as a unit, never empty. BMI specifically must use "kg/m²" or None — never any other invalid value. Confidence: 0.90
- Requests file-by-file walkthrough explanations after implementation is complete — wants a detailed breakdown of what each new file does and its role in the architecture, not just a high-level summary. Distinct from upfront architectural planning; this is post-hoc confirmation that the code matches the design. Confidence: 0.82
- When stuck or confused, asks "what i do [da]" as a signal that they want concrete, actionable next-step instructions rather than further explanation or rationale. Prefers a direct command ("run this script", "add these lines to .env") over additional context when blocked. Confidence: 0.85
- When a service credential (e.g., OpenAI API key) moves to Airflow Connections, the orchestration layer fetches it ONCE and passes it down as an explicit parameter into per-item processing functions (`process_one_file(file_record, openai_api_key=None)`), with the local config/env value (`settings.OPENAI_API_KEY`) retained as a fallback so local/manual runs keep working. Avoids re-fetching the connection per record and avoids spreading Airflow-specific credential lookups into the processing internals. Confidence: 0.55
- When the user says they can't understand ("i cannot understand say simple"), drops all technical depth and re-explains in plain, short sentences — simple words, no jargon, explicit pass/fail verdicts (✅ code is fine / ❌ data is bad), and a concrete "what to do" closing. Wants only the essence (who/what is at fault + the single next action), never the detailed chain of reasoning that lost them. Confidence: 0.78
- Expects every DAG present in the repo to be registered and visible in the Airflow UI — when only one shows up (e.g., "my airflow dag is only filenest" while arxiv/emr/medical DAG files exist), wants the cause investigated in the Airflow scheduler/dag_processor logs (import failures, missing modules in the Airflow venv, dags_folder mismatch) and the environment fixed so all DAGs appear, rather than accepting the partial list. Confidence: 0.62
- AI-generated clinical output is considered a draft and must never flow directly to downstream standards (FHIR, HL7, terminology servers). A Human-in-the-Loop (HITL) review gate with explicit approval is mandatory before any interoperability artifact is produced. Confidence: 0.90
- Standards generation (FHIR, LOINC mapping, SNOMED, ICD-10/11) is a separate, manually-invoked CLI step — never auto-triggered by the pipeline. The pipeline produces drafts; humans approve; then and only then does FHIR generation run. Confidence: 0.88
- Every human review action must produce an audit entry: reviewer identity, timestamp, field changed, old value, new value, and reason. The audit log lives inline with the clinical record, not in a separate system. Confidence: 0.85
- Once a clinical record is approved it becomes an immutable snapshot. The approved record is never mutated post-approval — any correction must create a new draft. Confidence: 0.82
- FHIR mappers follow strict one-mapper-per-resource-type: PatientMapper, ObservationMapper, ConditionMapper, MedicationRequestMapper, ProcedureMapper, DiagnosticReportMapper, ImagingStudyMapper — each has exactly one responsibility and knows nothing about other mappers. A separate BundleBuilder orchestrates them all. Confidence: 0.85
- Repository backends must be interchangeable via ABC interfaces — e.g., IFHIRRepository with LocalFHIRRepository for now, with HAPI/Azure/GCP as future plugins. The repository interface is abstract; implementations are concrete and swappable. Confidence: 0.80
- Clinical records follow an explicit workflow state machine: Draft → Pending Review → In Review → Needs Correction → Approved → Rejected. State transitions are logged in the audit trail. Only Approved records proceed to FHIR generation. Confidence: 0.82
- Prefers an interactive CLI pipeline UX: run the pipeline, see extracted/transformed data presented for review, edit fields in-place, then approve — not a read-only fire-and-forget batch process. The review step must support modification, not just inspection. Confidence: 0.80
- Pipeline entry-point scripts must be single continuous interactive sessions that carry the user from ingestion through review to final output (e.g., FHIR generation) in one invocation. Never require the user to run script A, inspect output, then remember to run script B — multi-step manual command chaining causes confusion and "what next" dead-ends. Confidence: 0.82
- Architecture additions must be strictly additive — never modify existing modules when adding a new layer. New layers compose around existing components without touching their internals. "Do NOT touch the existing Tool Registry" is treated as a strict, hard requirement — existing working modules (registry.py, base.py, mcp/) remain byte-for-byte untouched. Fresh evidence: the indexing spec's "Do NOT rewrite existing extraction, classification, transformation, FHIR-Staging, Jina, or OpenSearch implementations" and "Do not rewrite working code / Do not duplicate existing functionality / Do not introduce unnecessary frameworks / Do not make unrelated changes" — the assistant even noted a pre-existing factory/config mismatch but declined to touch it ("I won't touch existing working code"). Confidence: 0.94
- Prefers separate, purpose-specific pipeline entry-point scripts (e.g., `run_clinical.py` for clinical-only workflow) over a single monolithic pipeline script with mode flags. Each distinct workflow gets its own top-level entry point that imports only the components it needs. Confidence: 0.75
- Prefers strongly-typed FHIR R4 schemas using the `fhir.resources` library (Pydantic-based models, full field validation, proper resource types like `Patient`, `Observation`, `Condition`, `MedicationRequest`) over hand-rolled raw-dict FHIR representations. "Good schemas" means real typed models with built-in validation, not loose JSON. Confidence: 0.85
- Prefers repo documentation to be written as docstrings in the house format with `Purpose:` / `Args:` / `Returns:` sections, applied function-wise (every function and method), not just module-level or one-line docstrings. When asked to "add documentation", expects comprehensive per-function coverage using exactly that format (matching existing files like `graphbuilder/base.py` and `connectors/rdbms.py`). Confidence: 0.94
- Expects `__init__.py` files and package-level modules to also carry module docstrings describing package purpose and usage; files without any docstring are flagged as "MISSING" and get docstrings added proactively when the user requests documentation. Confidence: 0.72
- Reverts implementations quickly when they decide a feature isn't wanted (e.g., "can u reveret those implementation da" for a fully-built-and-verified clinical/staging/FHIR layer, then later requesting it be rebuilt; reverting an interactive CLI back to a batch script). Reverts should be surgical — remove only the new files and unwire their integration points, preserving unrelated improvements made along the way (the transformer refactor survived the clinical-layer revert). Confidence: 0.82
- Challenges code changes to something they believe is already working — "what are u changing its working" — and expects a precise, minimal-scope justification: exactly which file and which lines changed (before/after), and the concrete observed failure the edit fixes (e.g., the 422 Unprocessable Entity from the fhir-staging API on PATCH because observations weren't mapped to the API's input shape), not a general refactor rationale. The accepted answer confirmed the flow stayed identical except for the one required fix. Confidence: 0.68
- Prefers the EMR/clinical pipeline path (extract → transform → stage → review → FHIR) over the wiki knowledge-base path — explicitly does not want wiki, graph, or metadata steps in the clinical workflow and asks "what is the next emr" to steer toward clinical/EHR deliverables; confirmed the colleague-owned `fhir-staging` service as the pipeline's staging layer where transformed output lands before review/FHIR promotion. Confidence: 0.86
- Airflow DAGs are organized per-workflow inside `dags/unstructure/<module>/` — each pipeline (e.g., EMR vs medical) gets its own separate DAG file plus a `config/` subfolder with a `config.yml`, mirroring the existing DAG structure rather than folding the new workflow into an existing DAG. Confidence: 0.80
- Airflow DAGs must construct all components through the existing string-keyed factories (`ConnectorFactory`, `ExtractorFactory`, `TransformerFactory`) exactly like the rest of the platform — explicitly requested ("i want to use factory in my dags"). Direct class instantiation of concrete components (e.g., `RDBMSConnector`, `PyMuPdfExtractor`, `MedicalTransformer`) inside DAG code is replaced with factory lookups (`get_connector("rdbms", ...)`, `get_extractor("pdf", ...)`, `get_transformer("medical", ...)`); factories are the single composition point for orchestration code too, and any factory gaps surfaced during the refactor (e.g., `dicom` missing from the config-only branch) must be fixed in the factory itself, not worked around in the DAG. Confidence: 0.75
- Wants a simple unit test case covering the full workflow lifecycle (transformer → staging → review → approve) asserting state transitions, audit-trail entries (old→new values, reviewer identity), and approval gating — mock-based pytest fixtures are acceptable for workflow-logic tests, complementary to the real-file E2E verification scripts used for the extraction/transformation chain. Confidence: 0.70
- When a pipeline layer is proven working on the staging/approved version, expects it formalized with both a dedicated test case and a separate DAG file — "it's working on the staging, approved version" is the trigger to codify it with tests and orchestration, not to leave it unverified. Fresh evidence: the producer-DAG spec demanded verification after the change ("After making the changes, verify that the DAG loads successfully and explain the final producer → asset → consumer flow briefly") — the assistant imported the DAG with the Airflow venv and confirmed dag_id/schedule/outlets before reporting done; the indexing-layer spec prescribed its own verification workflow (chunker tests → indexer tests → compile/run checks), explicitly ordering "Add tests" BEFORE "add the separate Airflow indexing consumer", and demanded a final report listing files created/modified, architecture, chunking strategy, OpenSearch document structure, config changes, tests added, test/run commands, and assumptions. Confidence: 0.80
- Wants verification/flow scripts scoped to a single pipeline segment at a layer boundary, not the entire end-to-end flow — explicitly requested "only the before staging part" and expected the script trimmed to extract → classify → transform, ending with a "READY FOR STAGING" summary instead of continuing through staging, review, approve, and FHIR. Confidence: 0.85
- Runs DAGs in Airflow's runtime environment, which is a separate dependency universe from the local dev venv — a DAG that compiles locally can still fail in Airflow with `ModuleNotFoundError`. Expects the assistant to recognize such failures as an environment/installation issue (fix = `pip install` in the Airflow env, `docker exec` into the worker container, or adding to `requirements.txt`) rather than a code bug. Confidence: 0.70
- Scopes Airflow DAGs to the minimal pipeline segment currently wanted — when the user says "i just want before staging is enough", downstream stages (staging/review/FHIR) are removed from the DAG entirely rather than kept optional. Prefers scope reduction over adding new dependencies to the Airflow runtime (e.g., dropping FHIR stages instead of installing fhir.resources in Airflow). Confidence: 0.70
- Prefers real-file verification scripts at project root that run actual PDFs/DICOMs through the pipeline with live LLM calls, showing structured summaries (steps 1–3 with separators, abnormal observations highlighted) and ending at the layer boundary with a "READY FOR STAGING" marker — then explicitly requested the raw JSON be printed inline AND saved to a file (e.g., `storage/emr/transformed/{stem}.json`) in the same script. Confidence: 0.80
- Strict separation of integration boundaries: external teams own parts of the workflow (e.g., another app uploads files to FileNest), so the local layer must be read/download-only — no upload method at all, verified by a unit test asserting `not hasattr(downloader, "upload")`. Future stages (PostgreSQL, Airflow assets, dedup, scheduling, temp-file cleanup) are explicitly deferred with "do not implement yet". Confidence: 0.88
- The connector stays a pure infrastructure component with zero file/business logic (no upload, download, listing, temp-file management); file operations live only in the downloader, which is scoped to read/download: `list_files()`, `get_file()`, `get_download_url()`, `download_to_temp()`. Confidence: 0.90
- When the user rejects an existing behavior, expects the offending method (e.g., `upload()`) removed entirely, not kept alongside the new read-only API — the final interface must match the requested scope exactly with nothing extra. Confidence: 0.85
- Signed/temporary download URLs are regenerated fresh per download and never stored or persisted; files land in a configurable temp folder (default `storage/temp/`), auto-created with `pathlib.Path`, relative paths only — never hard-coded absolute machine-specific paths. Confidence: 0.85
- When weighing design options, asks "which is the good/best approach" and expects a decisive recommendation with justification — a clear "this is what I'd do and why", not a neutral menu of tradeoffs. Accepts the recommendation and asks for implementation (e.g., "okay da can u add the reports also"). Confidence: 0.65
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
 style file. Confidence: 0.90
- Prefers a single configuration system — never creates a second competing configuration mechanism; new config layers must reuse the existing conventions rather than introducing a parallel one. Fresh evidence: the indexing spec demanded all new settings (embedding provider/model, OpenSearch index name, max chunk size, chunk overlap, embedding batch size, indexing batch size) be added through the existing `PipelineConfig` with matching `.env.example` entries — "Reuse the existing configuration system... Do not hardcode credentials or secrets." Confidence: 0.88
- Avoids duplicating existing model classes — before introducing a new model (e.g., a new `ModelDefinition`), determines whether the existing one (e.g., `ModelConfig`) can be reused or extended. "Use the smallest clean abstraction" — never two models representing the same concept. Confidence: 0.85
- Registries should be keyed by logical names from configuration and decoupled from concrete types/enums — e.g., the Prompt Registry resolves logical prompt names ("clinical_extraction") to existing prompt content rather than being bound to `AgentType`; YAML `tools` lists logical tool names resolved by the existing Tool Registry (not a replacement `allowed_tools` concept). Confidence: 0.82
- Factories (e.g., AgentFactory) must be pure composition points — resolve model/prompt/tools from the respective registries and construct the object; they must contain no business logic. Confidence: 0.80
- Loads configuration once at application startup/lifespan and exposes it via `app.state.config` — never reads YAML/config files on every request. Confidence: 0.82
- Refactoring must preserve the existing directory structure — does not move files/agents into new directories merely for the sake of the refactor; the goal is making the existing framework config-driven, not redesigning the repository. Confidence: 0.85
- For implementation/refactor reviews, expects a final report with exact fixed sections: Files created / Files modified / Files intentionally untouched / Tests executed / Architecture after refactor / Backward compatibility status — and no unrelated changes. Confidence: 0.85
- Specifies new external-service integrations by pasting the vendor SDK's working usage snippet (client constructor arguments, method calls, env-var names) as the requirement spec — e.g., FileNest: `FileNest(api_key, project_id, base_url)`, `files.upload_from_path(..., mime_type=...)`, `files.get_download_url(id, ttl=...)` — and expects the new connector/downloader to wrap that exact API surface with credentials read from environment variables. Confidence: 0.70
- When a runtime reports `ModuleNotFoundError`, expects it to be diagnosed as an environment/installation issue (install the package into the right venv/container), never a code defect — including internal/vendor SDKs not published on public PyPI (e.g., `filenest`), where the fix requires locating the package (local wheel, private registry, git URL) and asking the user where it lives rather than changing the connector code. The user answers by supplying the local SDK path (e.g., `~/filenest-python-sdk`) so it can be installed editable. Confidence: 0.80
- The project runtime is a venv (e.g., `/home/kipla/aipipeline/venv`), not the system Python — dependencies installed with system `pip3` are invisible to the runtime, so installs must target the active venv (`<venv>/bin/pip install -e .`) and be verified with the venv's own interpreter. When a manual test fails with `ModuleNotFoundError`, check which interpreter the shell resolves (`which python3` / `sys.prefix`) before touching code. Confidence: 0.78
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
g task work — explicitly asks the assistant to "go through" specific directories (src, dags, storage) or explain existing pipeline concepts first (e.g., "can u go through the src, dags and storage da", "can u gothrough the concepts"). Expects a thorough architecture summary before feature or bug work begins — also directs the assistant to "go through" a specific file/module (e.g., "u gothrouth the utils/config.py", or a whole folder: "go through the chuncking inside the indexing folder") to ground the answer in the actual code rather than paraphrase. Also recurs as an escalation: after receiving a simple plain-language explanation of how something works ("how is this chunking"), the follow-up ask is often "go through" the real implementation — expecting a file-by-file walkthrough grounded in what the code actually does. Confidence: 0.80
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
stage implements only its own responsibility (e.g., read/download only); future stages (PostgreSQL, Airflow assets, dedup, scheduling, temp-file cleanup) are explicitly deferred with "do not implement yet" rather than scaffolded prematurely. Confidence: 0.85
- Unit tests must never require real external credentials — external SDK clients are mocked and filesystem tests use temporary directories; real-credential verification is kept in separate manual test scripts (e.g., `tests/manual/`) that load config from environment variables. Confidence: 0.80
- Treats the external service's SDK/API id as the unique file identifier, never the human-readable filename — duplicate filenames are expected and must not be used as keys; display/storage names are derived from actual metadata. Confidence: 0.75
- Temporary/signed download URLs are regenerated fresh for every download and never persisted — expiring credentials/URLs must not be stored. Confidence: 0.70
- Downloads from external sources go to a configurable temporary staging directory (default `storage/temp/`) that is auto-created via `pathlib.Path`; paths must be relative/configurable, never hard-coded absolute machine-specific paths. Confidence: 0.70
- Wants a detailed, current README.md at the project root for the GitHub repo, aimed at new contributors — explicitly requests "detailed docs" ("add me a readme.md file new to this github its will helpfull for them") and expects the README to be comprehensive: overview, architecture diagram + component table, installation (venv + requirements.txt + internal SDK note), full `.env` example, quick-start commands, graph backends, testing, storage layout, and HITL workflow. When a rewrite replaced the original README, expects the ORIGINAL content restored and preserved, with the new usage sections appended below it ("hey my old readme.md file i want and add the existing usages also okay") — documentation changes should be additive to the existing text, never a wholesale replacement of content the user already wrote. Confidence: 0.85
- Reuses existing database access patterns when adding a persistence layer — when the codebase uses raw SQLAlchemy `Connection` objects (no ORM Base, no sessionmaker, no Alembic), new repositories must be built on that same raw-connection/parameterized-SQL convention rather than introducing a second ORM/session architecture. Explicitly forbids creating a new `PostgresConnector` when a generic RDBMS connector already exists, and forbids a second SQLAlchemy `Base`. Confidence: 0.85
- Dynamic/unstructured fields (e.g., external vendor file metadata) must be stored in a single JSON/JSONB column, never flattened into separate database columns — "do not flatten these dynamic metadata fields into separate database columns." Confidence: 0.80
- Existing working infrastructure components (connectors) must not be rewritten when adding a new layer — if a modification is genuinely required, it must be the smallest possible backward-compatible change with an explicit explanation of why it is required (e.g., a dialect-aware tweak so a connector can also drive SQLite tests), never a rewrite. Confidence: 0.85
- Expects proper database transaction/resource hygiene — transactions explicitly committed (with rollback on error), connections closed via context managers/`finally` so nothing is left open, and no leaked connections or open transactions. Confidence: 0.75
- Breaks large feature tasks into explicit incremental scopes, each delivered and verified separately — e.g., "implement these incrementally" for repository methods, and "Do NOT implement processing state yet / Do NOT add Airflow yet" gates future stages until the current one (e.g., PostgreSQL persistence) is proven working. Confidence: 0.75
- For database layers, wants a separate manual integration test (distinct from unit tests) that connects to the real database and real external service, persists one real record, reads it back, and prints it — keeping repository verification separate from downloading/processing. Unit tests never require real credentials. Confidence: 0.70
- Wants a single full-flow integration test for the external download/persistence chain — one script (e.g., `tests/manual/test_filenest_full_flow.py`) that runs connector → list_files → get_file → get_download_url → download_to_temp → repository save → update download status → read-back verification in one pass, using real credentials and real PostgreSQL, printing step-separated output and accepting an optional file_id argument. Explicitly requested on top of the repository-only test ("full test case like filenest connector, get url, postgres download alike that") — the complete scenario, not just one segment. Confidence: 0.72
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
le (e.g., `LocalCredentialProvider("rdbms").get_credentials()`) — never through a newly-invented helper function added to `config.py`. When a `get_rdbms_config()` helper was introduced there, the user demanded a revert and pointed to the existing pattern: "not like this revert this changes u go to the utils inside config u get that". `config.py` remains the static `CONNECTIONS` dict; the provider class is the single access point. Confidence: 0.85
- Wants PostgreSQL credentials to be created through `src/components/utils/config.py` — explicitly names `utils/config.py` as the place for credential creation ("i want createntials for postgres using utils/config.py for this creation"), preferring a reusable utility in the utils config module over inline construction in scripts, a new helper in `credentials/local/config.py`, or the reverted `get_rdbms_config()` approach. Refined further: each infra service's config must live INSIDE the `PipelineConfig` class exactly like Neo4j/ArangoDB — bare env-var fields (`POSTGRES_HOST`, `POSTGRES_PORT`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`), a `get_postgres_config()` method returning the connection dict ready for the connector, and a `postgres_enabled` property — not a standalone module-level function ("no i want same config like neo4j like that postgres inside the config class"). Manual/integration tests are expected to consume these credentials via `PipelineConfig().get_postgres_config()`. Confidence: 0.78
- Prefers bare, unprefixed environment variable names in `.env` — explicitly rejected the `MED_WIKI_` project prefix ("i donot want prefix med_wiki like that can u change that"), expecting `MED_WIKI_POSTGRES_HOST` → `POSTGRES_HOST`, `MED_WIKI_POSTGRES_USERNAME` → `POSTGRES_USER`, etc. Infrastructure env vars should use generic service names (POSTGRES_*, NEO4J_*) without project-specific prefixes, and when renaming them expects every consumer (config module, `.env`) updated consistently with tests still passing. Confidence: 0.75
- Hands over environment/credential configuration to the assistant — when asked for a missing secret (e.g., the real local PostgreSQL password after repeated auth failures), replies with the raw value in chat ("my password is Aleesha@1412") and expects the assistant to write it into `.env`, re-run the failing test, and keep diagnosing (config read-back, process checks, auth mode) until it passes, rather than editing the config file or running the manual fix steps themselves. Confidence: 0.55
- Expects full-flow verification/integration scripts to process ALL files in the source list in one run, not just the first file or a single sample — flagged "total 10 filles but one is proccessed y" (total 10 files but one is processed, why) as unexpected when the end-to-end test only chained through one file even though `list_files()` showed all ten. Explicitly confirmed the desired behavior when offered: "yeah i need process the new file one by one for single run" — files are processed sequentially (one by one) in a single run, looping over the whole list (e.g., an `--all`/loop mode reusing one DB connection), not demonstrated on a single sample or split across multiple runs. Confidence: 0.75
- Airflow DAG file output must use absolute paths derived from the project root (e.g., `_AI_PLATFORM` or config `project_root`/`output_dir`), never relative paths — Airflow workers run from a different working directory, so a relative `storage/emr/transformed` silently wrote to the wrong location and the user reported "the final otput doesnot save my local da storage/emr/transformed". Fresh evidence: asked the assistant to audit every DAG config for relative paths ("hey check dags unstructure the config.yaml file is relative path is ther") and then demanded all of them be made absolute ("hey i need abosolute path") — every `download_dir`/`output_dir`/`image_output_dir` in the unstructure DAG configs (filenest/emr/medical) and their runtime fallbacks must resolve to an absolute path rooted at the project root. Confidence: 0.80
- Wants the complete ingestion chain driven inside Airflow — "i need all in airflow da": the producer DAG should orchestrate connect → list → download → PostgreSQL persist → emit Asset, not just check a condition and emit events while the actual download/persist work happens in manual scripts. Reconfirmed when asking for "end to end processing" and specifying the full consumer flow (pending records → PATCH processing → download → extract → classify → transform → PATCH completed) entirely inside the DAG. Confidence: 0.75
- Output artifacts for duplicate-named files must be collision-free — include the unique external id in artifact filenames (e.g., `{stem}_{file_id[:8]}.json` on save, `{file_id[:8]}_{filename}` on download) so same-named files (multiple `check.pdf` with different FileNest ids) don't overwrite each other on disk. Confidence: 0.70
- Medical image formats (PNG, JPG, WEBP, TIFF, BMP) should be supported in the medical processing pipeline via vision analysis (ImageAnalyzer / GPT-4V) — asked "y ia this unsupported da" when images were skipped, and expects image descriptions to feed the classifier/transformer like text does, not to be rejected as unsupported file types. Confidence: 0.70
- Processing DAGs must handle bad records gracefully instead of failing the whole run — files missing on disk (stale DB records), corrupt files, and unsupported types are skipped with logged warnings per record, so one bad file never crashes the entire batch. Confidence: 0.65
- Airflow DAGs source credentials for external services (FileNest, Postgres) from Airflow Connections (Admin → Connections) via the existing `CredentialFactory.get_provider(mode="airflow", conn_id=...)` pattern — explicitly requested ("i need to use airflow credentials for this dag like filnest, postgres related things can u understnad what i say"). Conn IDs are declared in the DAG's `config.yml` under `credentials:` (`mode`, `filenest_conn_id`, `postgres_conn_id`) and kept configurable rather than hardcoded; the DAGs retain env-var fallbacks (and `base_url` falls back to the connection's Host field) so a missing connection doesn't break runs. The user confirmed creating the connections in Airflow using exactly the config-declared IDs ("i update in my airflow connection the filenest, conn_id is filenest_conn_id, and the postgres is postgres_conn_id"), so the config.yml conn IDs are treated as the source of truth for what gets provisioned. This supersedes an earlier .env-only approach (load_dotenv + FILENEST_*/POSTGRES_* env vars). Fresh evidence extends this to LLM provider secrets: when the user asked "hey can u use openai airflow credentials", the OpenAI API key was moved from `.env`/`PipelineConfig.OPENAI_API_KEY` to the Airflow `openai` connection (declared as `credentials.openai_conn_id` in config.yml), fetched once via `CredentialFactory.get_provider(mode="airflow", conn_id=...)`, read from connection extras (`api_key`, falling back to `password`/`login`), and injected as a parameter into `process_one_file()` for all LLM consumers (image analyzer, classifier, transformer) — with `.env`/PipelineConfig retained as fallback when the connection is missing, so the same service-credential-from-Airflow rule applies to API keys, not just hosts/secrets of storage services. Fresh evidence extends this to the embedding/indexing layer: when the user asked "i want temp/ images also and i need to add the airflow credentials its very helpful i think", the direct-indexing DAG moved Jina (embedding API key) and OpenSearch credentials into Airflow Connections — a `_build_indexer_config()` helper fetches them once via `CredentialFactory` and overrides `PipelineConfig` (jina: `api_key` extra → `JINA_API_KEY`, `host` → `JINA_BASE_URL`; opensearch: `login`/`password`/`host`/`port`/`schema` → `OPENSEARCH_*`), with conn IDs declared as `credentials.jina_conn_id` / `credentials.opensearch_conn_id` in config.yml, a warning log, and `.env` fallback when a connection is missing so runs never break. Fresh evidence: when the assistant used placeholder IDs (`jina`/`opensearch`), the user supplied the real provisioned IDs directly ("airflow conn id is opensearch_api, jina_api") and expects BOTH the config.yml `credentials.*_conn_id` entries AND the DAG code's fallback defaults updated to those exact IDs so code and config agree — the config-declared conn IDs must match what actually exists in Airflow, and the assistant verified the change by loading the DAG and printing the resolved IDs. Further evidence: the `jina_api` connection's decrypted extra carries NO `api_key` key at all — the API key lives inside a `headers` dict (`Authorization: Bearer <key>`) and the full endpoint is provided via a `base_url` field (with `model`/`dimensions`/`task_passage`/`task_query`/`batch_size` also in the extra); after the DNS fix, a 401 from `https://api.jina.ai/v1/embeddings` traced directly to the key never being read, so credential extraction must search multiple candidate locations (`api_key` → `token` → `password` → parsed `headers.Authorization` → `login` only when it looks like a key, e.g., starts with `jina_`), and the base URL should be taken verbatim from the connection's `base_url` when present, falling back to a normalized `host`. Confidence: 0.84
- Wants standard open-source repo documentation beyond the README — explicitly requested a `CONTRIBUTING.md` covering dev setup, architecture conventions, code style (Purpose/Args/Returns docstrings), testing, DAG/Airflow patterns, env/secrets handling, and PR process, written to match the repo's actual conventions. Confidence: 0.65
- Uses Airflow 3 Assets (`from airflow.sdk import Asset`) for event-driven pipeline scheduling — one logical Asset per data collection (e.g., "downloaded medical files available"), never per-file; a producer DAG emits the Asset via task `outlets=[asset]` only when the condition is satisfied (e.g., downloaded files exist in PostgreSQL), and the consumer DAG subscribes via `schedule=[asset]`. The first implementation deliberately avoids per-file assets, AssetWatchers, sensors, and polling — kept as the simplest possible working example. The emit-only-on-success semantics were confirmed operationally during a cross-environment diagnosis: when the producer's downloads fail, no Asset is emitted and the consumer never runs. Reconfirmed by a detailed producer/consumer spec (producer emits the shared `medical_files_asset` via `outlets`, consumer subscribes via `schedule=[asset]`, both build the identical Asset from config). Confidence: 0.72
- Integrates with external/colleague-owned services as the staging layer via a thin HTTP bridge rather than reimplementing that service in-repo — e.g., a client (`FhirStagingClient`) plus a schema mapper (`StagingObservationMapper`) that converts the transformer's Clinical Domain Model observations into the external API's input shape, then POSTs the file record and PATCHes the observations back (status pending → completed). Accepts that making the integration work may require fixing the external service's own environment (creating its database, running its Alembic migrations, correcting its `.env` DB URL encoding). Confidence: 0.72
- Every write path to an external/colleague-owned API must conform to that API's validated input schema exactly — when the consumer PATCHed raw transformer observations back to fhir-staging it was rejected with `422 Unprocessable Entity` on `PATCH /api/v1/staging-records/{id}`; the fix was mapping observations through the existing `StagingObservationMapper` (the same path the push service already used) to produce the `ObservationInput` shape (`code_display`, `value_quantity_*`, `category: [{coding_*}]`, `reference_range: [{low_value, high_value, text}]`, `interpretation: [{coding_*}]`). Reuse the existing mapper rather than sending raw internal shapes. Fresh evidence: adding a per-observation `summary` key still 422'd because `StagingObservationInput` declares `additionalProperties: false` — that field exists only on the top-level `StagingMedicalRecordPatchSchema`, so it must be sent at the top level of the PATCH payload (`patch_payload["summary"] = ...`), never replicated inside each nested observation item; the mapper must emit only fields the item schema actually declares. The user supplied this exact root-cause diagnosis (mapper.py:90-93, OpenAPI spec) as the spec to implement. Confidence: 0.78
- Every new field added at the transformer/domain layer (e.g., per-observation `ai_summary`) is expected to flow end-to-end into the external staging API — "i need to push into the staging api can u understand" — i.e., the staging mapper must be extended to carry the new field into the external API's input shape (assistant mapped `ai_summary` → FHIR `note`) so it is PATCHed back with the observations, never left as an internal-only domain field. Fresh evidence: after the mapping was explained, the user reiterated "just can u add the summary" — the terse directive confirming the summary must actually land in the pushed payload, not merely exist internally — and the end-to-end verification (transformer observation → mapper → PATCH payload with `note`) was the accepted proof. Confidence: 0.70
- Wants the AI-generated per-observation summary mapped into the external staging API under the self-explanatory field name `summary`, not FHIR's `note` — after the assistant explained that `note` is the FHIR-standard field the colleague's API actually validates, the user still directed "can u change the mapping note into summary okay". Prefers intuitive, self-descriptive field names over opaque standard-conformant names, even when the downstream schema only accepts the standard name; expects the naming conflict resolved by coordinating with the API owner to add `summary` to their schema rather than silently keeping `note`. Fresh evidence: when the assistant pushed back because the live API still validated only `note`, the user closed the debate by asserting the schema change is in the colleague's hands and insisting the mapper be switched anyway — "my colleague add summary so u donot worry i want summary fields". The user's word that the external schema is/will be updated is treated as sufficient to change the code immediately, even before the live API serves the field (the PATCH may 422 until the colleague deploys, which the user accepts). Confidence: 0.78
- When the user asks to "see" a running service (e.g., "can i see 8002", "can i see the values"), wants the actual data surfaced directly — browser-accessible interactive views (FastAPI/OpenAPI docs UI like `http://localhost:8002/docs`), direct curl commands, and clean human-readable dumps of real records (observation values with units, reference ranges, abnormal flags), not just a process/port confirmation or descriptions — reinforced when the user asked "just explain where i use to check this records" and the answer again led with the browser-accessible Swagger docs UI at `http://localhost:8002/docs`. Confidence: 0.68
- Treats `.env` as the canonical credential store: when configuring Airflow Connections for an external service (e.g., FileNest), asks the assistant to "give me the credentials" and expects them retrieved from the project's `.env` and handed back in ready-to-paste form — the exact Extra JSON for Admin → Connections plus the equivalent `airflow connections add` CLI command — rather than being told to find the values themselves. Confidence: 0.55
- When asking a clarification/confirmation question about current behavior (e.g., "can u clarify me that dags only processing new file no process the alreddy processed file ... just tell me"), wants a direct, concise answer first — confirm understanding and state plainly what the code actually does — before any implementation is proposed or started. "Just tell me" signals answer-the-question, not start building or investigate at length. The same applies to outcome-verification questions: when the user pastes a run log and asks "i have a doubt, is this the result, did it transform the file?", they want a definitive plain-language verdict on whether the run achieved its goal (e.g., "No — this run did NOT transform any file; the log shows it returned early with `[]`"), with the log lines interpreted, before any next-step options are offered. Fresh evidence: pasted a colleague's full Airflow consumer task log labeled "my collegue got this error" when the run actually SUCCEEDED (0 pending records, clean exit, `Returned value was: []`) — expects the assistant to first correct the misreading ("that log is not an error — the consumer ran perfectly"), then distinguish benign log noise (deprecation/shadowing WARNING lines) from real signal, and only then trace where the true problem lies (producer-side failures). Fresh evidence: asked "this is a note right" while pasting a stored `note` JSON from the staging API — a yes/no confirmation about data shape that expects a direct "Yes, that's the note" answer identifying what the payload is and where it came from, before any further detail. Fresh evidence: asked "what did u say its working or not" immediately after a long diagnostic summary — a terse status-verification question expecting a direct plain-language verdict, not a re-run of the technical detail; the accepted answer opened with a definitive "Partly — the wiring is working, but no document has fully indexed end-to-end yet," split verified components (✅) from what hasn't completed (❌), and closed with a concrete offer to prove it end-to-end with a clean test record. Confidence: 0.78
- Explicitly parks the current discussion topic and redirects the conversation when switching focus — "okay u hold this da we discuss about staging area" — expects the assistant to follow the redirect immediately, explain the newly-named topic (e.g., the staging flow) in simple terms, and not keep returning to the parked topic until the user brings it back up. Confidence: 0.55
- When asking where something lives in the codebase (e.g., "can u return the file name where i use this da postgres related"), expects a categorized file-path map for that concern — entries grouped by role (config/credentials, repository/table logic, external-service integration client+mapper, scripts/DAGs that use it), each with a one-line description of what the file does — so they can navigate to the exact file to work on, not just a single file name or an explanation. Confidence: 0.65
- When the user asks "where [he] want change" after a diagnosis (e.g., where a colleague must fix a bad `file_id`), wants the exact fix location stated plainly — the specific table/column or code file — plus copy-pasteable quick-fix commands (e.g., a direct `UPDATE staging_record SET file_id = ...` on the colleague's DB) to unblock immediately; a direct database edit is accepted as a valid alternative to fixing the app code that wrote the bad data. Confidence: 0.55
- When an optional external/colleague-owned service (e.g., fhir-staging) is down or disabled, the user does want the pipeline to still produce output (complained "its is empty" when the consumer returned nothing) — BUT explicitly rejected the proposed fix of rewriting the DAGs into a dual-mode fallback architecture (a PostgreSQL-driven Mode A fallback branch added alongside the existing path): "no i revert the old method" demanded the original method be restored as-is (restored from the committed version), and the producer rewrite was denied outright. Do not restructure working DAG code into Mode A/Mode B branches without explicit sign-off; prefer the smallest fix to the existing method over introducing parallel fallback modes. Confidence: 0.65
- After accepting an implementation ("its okay good"), requests pytest unit tests for the whole feature area — expects mock-based, credential-free tests covering every component of that area including newly added integration layers (e.g., fhir_staging mapper, client, push service with mocked httpx), not just re-running pre-existing tests; the new and existing suites must be run together and all pass before the work is reported done. Confidence: 0.70
- Wants dependency installs and runtime imports scoped strictly to their actual use case — "hey i donot need that" / "i only need my usecase specifics only" explicitly rejected installing the full transitive set of connector deps (google-auth, google-api-python-client, elasticsearch, opensearch-py, redis, arango) merely to satisfy an eager import chain in a manual test. Install/import only what the use case actually uses, nothing extra. Confidence: 0.85
- Test runs should be scoped to the use-case-specific test suites only, not the full repo `tests/` directory — "i only need test my usecasespecif only this time" after the full-suite run hit pre-existing collection/import gaps (Spark needs pyspark, test_emr needs email-validator, manual scripts need live creds). Verification means running just the suites covering the current feature area (e.g., fhir-staging bridge, FileNest, repository persistence) and treating those targeted results as the real signal, without chasing/fixing the whole repo's baseline gaps. Confidence: 0.75
- Narrows work/investigation scope to the specific files named for the current task — when the assistant was investigating why only the filenest DAG appeared in Airflow, the user interrupted with "can u only focus on the filenest inside 2 files is enough" to stop the whole-repo/DAG survey and limit attention to the two filenest DAG files (asset_producer.py, asset_consumer.py). Expects explanations and fixes grounded in that minimal file set, not broader architecture investigation. Fresh evidence: when the pipeline was stuck, the user pointed directly at the culprit component ("the probblem is producer check carefully its update status"), steering the investigation to the producer's status-update code rather than letting the assistant continue down a data-side diagnosis. Confidence: 0.72
- Asserts root-cause hypotheses about their own system and expects the assistant to take them seriously and verify against the code — "the probblem is producer check carefully its update status" redirected an investigation that had been pointed at bad data (a malformed file_id) back to the producer's status-update logic, and the user was right: the producer claimed records as `processing` and never released them, deadlocking the consumer. When the user names the suspect, re-examine that exact code path carefully before pursuing other theories. Confidence: 0.60
- Package `__init__.py` files must not eagerly import every subcomponent — a `connectors/__init__.py` that imports all connectors (gmail, arxiv, elasticsearch, opensearch, arango, ...) forces pulling in heavy unused dependencies at import time and breaks unrelated usage: importing the package failed with `ModuleNotFoundError: No module named 'arango'` even for FileNest-only work. The fix expected: `__init__.py` eagerly imports only the core connectors every pipeline uses (RDBMS + FileNest) plus the factory, optional connectors stay reachable via the factory, and each optional connector lazy-imports its SDK inside `connect()` (with a helpful `pip install` message) so a missing optional dependency can never break package import — the same lazy-import pattern FileNestConnector already used. Confidence: 0.85
- Prefers a strict producer/consumer split in Airflow Asset DAGs: the producer DAG is a pure asset trigger — connect to the work-source API (fhir-staging), fetch pending records, log each one with its `file_id`, and emit the shared Asset via task `outlets` — and must NOT download files, create a connector/downloader, or touch temp files (spec: "DO NOT download files from FileNest / DO NOT create a FileNest connector or FileNest downloader / DO NOT create temporary files — the producer should only act as an asset trigger"). The consumer DAG (subscribed via `schedule=[asset]`) is the only component that downloads and processes. The user expects the implementation verified against these constraints (grep evidence: producer has 0 `download_to_temp`, consumer is the only downloader, both build the identical Asset from config), with unused imports cleaned up. Fresh evidence: the producer-update spec reiterated the same split (producer "must NOT download files from FileNest / must NOT create a FileNest connector or downloader / must NOT create temporary files") and added that retries/retry-delay stay sourced from `config.yml` ("Keep retries and retry delay from `config.yml`") rather than being hardcoded into the DAG. Confidence: 0.80
- Communicates large feature requirements as detailed structured specs — numbered REQUIRED FLOW / FAILURE FLOW, an IMPORTANT "DO NOT" constraint list (do not create new tables, do not use X repository, do not add a new status), FILES TO CHANGE, exact credential field names, and a final verification checklist ("Also verify that: the producer does not contain `download_to_temp()`...") — and expects the implementation to match the spec exactly, reusing only the factories/patterns the spec names, and to report just the files changed plus confirmation of each stated constraint. Fresh evidence: the producer-DAG update arrived as a 12-point numbered requirements list with an explicit "After making the changes, verify that the DAG loads successfully" step; the document-embedding/indexing feature arrived as a 15-section spec (inspect existing code first → chunker → chunk model → Jina → OpenSearch → idempotency → indexer service → config → tests → Airflow integration last) with "DO NOT rewrite / DO NOT duplicate / DO NOT introduce unnecessary frameworks" rules, an EXPECTED FINAL ARCHITECTURE diagram, and a prescribed final-response format. Confidence: 0.72
- Wants AI-generated plain-language summaries in the observation layer of the clinical domain model — requested "i want to add the ai summary in my observation layer"; each `Observation` carries its own `ai_summary` (1-2 sentence clinical interpretation of the finding, e.g. "Hemoglobin is below the normal range, consistent with anemia — correlate clinically"), distinct from the existing document-level `summary`. Expected implementation spans the Observation model (new field), the observation builder (passthrough from LLM output; vitals-derived observations default to `None`), and the clinical extraction prompt (instruct the LLM to generate it per observation). Confidence: 0.55
- When asked to explain the flow they built OR how an existing component works (e.g., "can u explain the flow we build, explain simply", "can u tell me how is this chunking"), expects a simple, plain-language explanation — not an architecture lecture or a code walkthrough: lead with the core idea in one line, show the structural mapping visually (ASCII trees/arrows, e.g., which document sections become which chunks), give a concrete before/after example (input JSON → rendered chunk text), then a short list of key rules/invariants, closing with why it matters for their actual use case and an offer to drill into detail. Confidence: 0.65
- Mid-work, periodically asks "what we are working da" to re-orient on the current project state — expects a concise plain-language recap covering: the pipeline being built (one-line flow), what was recently done/fixed, the current state, and the single remaining next step — not an architecture lecture. Distinct from the "explain the flow" request: this is an orientation/status question to re-anchor before continuing. Confidence: 0.5
- Asks direct yes/no questions about which infrastructure a flow actually uses ("in this case we are using postgres database or not da") and expects the answer verified against the code, stated definitively, with a clear ownership split — local/our components vs. colleague-owned external services (e.g., the PostgreSQL behind the fhir-staging API is the colleague's, not ours) — and a note when previously-built components (e.g., the old FileNestRepository + `filenest_files` table) are no longer part of the current flow. Confidence: 0.55
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
 `PipelineConfig` — env-var field (e.g., `FHIR_STAGING_BASE_URL`) plus a `get_*_config()` getter returning the connection dict, with a matching `.env` entry — never hardcoded inside client/component code. Explicitly asked to move `client.py`'s hardcoded `http://localhost:8002` into `config.py` ("change this client.py base url into config.py its gets from that") so the client reads it from there; explicit constructor overrides may remain as opt-in. This extends the postgres/neo4j/arango config pattern to every new external service. Confidence: 0.75
- External/colleague-owned API schema field names must be config-driven too, not hardcoded literals in mapper/client code — explicitly requested ("can u clarify can u add the schema-record into config driven because in future change this into another name so i ask da") because the colleague renames fields (like the earlier `note` → `summary` change) and future renames should touch only config, never code. Expected shape: a `PipelineConfig` env-var field with a sensible default (e.g., `FHIR_STAGING_SUMMARY_FIELD: str = "summary"`) exposed via the service's `get_*_config()` getter, read by the mapper (and the client's update paths — status/updated_by/error_message field names included) instead of hardcoding `summary`. Extends the config-driven pattern (base URLs in config.py) to payload field names. Confirmed with a terse "okay" when the assistant proposed the config plan. Confidence: 0.80
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
ry new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
nstructor overrides may remain as opt-in. This extends the postgres/neo4j/arango config pattern to every new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
ry new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
e: 0.65
Confidence: 0.65
65
ry new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
ternal service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
5
 unverified. Confidence: 0.65
is extends the postgres/neo4j/arango config pattern to every new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
ry new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
nstructor overrides may remain as opt-in. This extends the postgres/neo4j/arango config pattern to every new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
ry new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
e: 0.65
Confidence: 0.65
65
ry new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
ternal service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
5
 unverified. Confidence: 0.65
ides may remain as opt-in. This extends the postgres/neo4j/arango config pattern to every new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
ry new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
e: 0.65
Confidence: 0.65
65
ry new external service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
ternal service. Confidence: 0.75
 unverified. Confidence: 0.65
ence: 0.65
 unverified. Confidence: 0.65
5
 unverified. Confidence: 0.65
 unverified. Confidence: 0.65
- For embedding/indexing pipelines, requires semantic, structure-aware chunking with strict null/empty-value exclusion — the spec's `ClinicalDocumentChunker` must convert Clinical JSON into meaningful chunks (never embed raw JSON), skip `None`/empty strings/empty lists/empty dicts via reusable logic without modifying the original Clinical JSON, prefer one semantic chunk per observation (e.g., `Observation: Heart Rate / Category: Vital Signs / Value: 78 bpm / ...`), and split oversized sections only via a configurable secondary text splitter (max chunk size and overlap configurable — "Do not hardcode limits throughout the code"). Confidence: 0.78
- Indexing must be idempotent with deterministic chunk IDs and upsert semantics — re-processing the same Clinical JSON must not create duplicate chunks; chunk identity derives from stable information (file_id + chunk_type + stable section identifier, hashing if necessary) and re-indexing upserts existing OpenSearch documents rather than appending. Extends the pipeline-idempotency preference to the embedding/indexing layer. Confidence: 0.72
- Vector-similarity retrieval is not sufficient alone for medical document chat — every indexed chunk must store `patient_id` and `file_id` as structured, filterable OpenSearch metadata fields (not just inside the embedding text) so retrieval can filter by patient_id AND file_id before vector search. Confidence: 0.68
- Embedding vector dimensions must match the configured model exactly — the spec explicitly warned "The embedding vector dimension must match the configured Jina model. Do not hardcode an incorrect dimension", so dimension settings come from config (e.g., `EMBEDDING_DIMENSIONS`) alongside the model name, never a hardcoded literal that can drift from the model. Confidence: 0.65
- The canonical `.env` holding real credentials for the pipeline lives one directory above the project root (`/home/kipla/aipipeline/.env`, not `aiplatform/.env`) — when the user says "can u see the route .env file" and then answers with that parent path, it means "that parent file is the source of truth for credentials." The project `.env` (which `PipelineConfig` actually loads) may lack them, so the chosen wiring is to copy the credentials into the project `.env` (the user selected this option over making `PipelineConfig` load the parent file or keeping a separate naming scheme), matching how `POSTGRES_*`/`ARANGO_*` already work. Confidence: 0.70
- Config field names must match the env var names already present in the canonical `.env` — e.g., `JINA_MODEL`, `JINA_DIMENSIONS`, `JINA_TASK_PASSAGE`, `JINA_TASK_QUERY`, `JINA_BATCH_SIZE` — rather than inventing parallel names (e.g., `EMBEDDING_MODEL`, `EMBEDDING_DIMENSIONS`, `EMBEDDING_TASK_PASSAGE`); when the newly-added config names mismatched the user's existing `.env` variables, `PipelineConfig` was renamed to the existing convention so there is a single source of truth. The `.env` may also be updated externally (sync process/colleague) with the parent's variable names, so re-read it before editing rather than assuming prior state. Confidence: 0.60
- Airflow 3 asset-event DAGs must use the real task-context mechanism for event data — `context["inlet_events"]` to consume and `context["outlet_events"][asset].extra.update(...)` to attach per-event metadata (file_id, patient_id, filename, filepath, content_type) — never a fake helper and never `NotImplementedError` placeholders. The direct-indexer spec explicitly forbade a fake `get_asset_event_data()` helper ("Use Airflow 3 asset-aware task context correctly... through the supported Airflow task context/inlet_events mechanism") and demanded the event carry the file info so the DAG can process the actual downloaded file without re-fetching. Confidence: 0.70
- Async integrations called from a sync Airflow task must run on ONE asyncio event loop for the whole operation and release resources deterministically — the direct-indexer spec required "Use asyncio correctly. There should be ONE asyncio event loop for the indexing operation" and "Properly close the indexer after indexing" (an `indexer.close()` in a `finally` block that closes the underlying connection, handling async closers). Avoids nested/duplicated `asyncio.run()` calls that would create multiple loops. Confidence: 0.65
- Service classes that hold live connections (embedding providers, indexers) should expose a `close()` method so callers can deterministically release resources in a `finally` block — the indexer's `close()` is best-effort, closes the underlying connector (running an async closer via `asyncio.run` when needed), and logs a warning on failure rather than silently swallowing it. When an orchestration spec says "properly close the indexer", the missing close method is added to the service itself, not stubbed or skipped. Confidence: 0.62
- Direct-indexing orchestration must not add filesystem-based state tracking when the underlying layer is already idempotent — explicitly forbidden: "Do not add processed.json or filesystem-based state tracking. OpenSearch/indexer idempotency should be handled by the existing indexing layer." Dedup belongs to deterministic chunk IDs + upsert semantics inside the indexing layer, not to filesystem bookkeeping in the DAG. Confidence: 0.62
- For semantic, structure-aware chunking: chunk text is rendered as clean `Label: value` lines (e.g., `Observation: Heart Rate` / `Category: vital_signs` / `Value: 78 bpm` / `Interpretation: normal` / `Effective Date: ...`), normalized whitespace, and rendered per item — never raw JSON. Null/empty values are skipped in the text renderers AND via the reusable `_is_not_empty` check, and a chunk with no usable name/content is dropped entirely (e.g., an observation with `value=None` still chunks under its display name, but a diagnosis with no name yields no chunk). Confidence: 0.68
- When the semantic chunker splits oversized text, each sub-chunk must be preserved and given a deterministic, distinct id — sub-chunks get a `#0/#1` suffix appended to the stable identifier inside the hashed chunk id so re-chunking the same document yields identical ids. Dropping the overflow sub-chunks (keeping only `sub[0]`) is a bug: the tests expect every piece under `max_chars` to be emitted. Confidence: 0.62
- Do not modify `config.yml` unless absolutely required — a DAG that needs shared identity/settings should read the existing config files (e.g., the direct document indexer reuses the filenest config's asset identity and download dir) rather than adding parallel config sections; "Prefer existing configuration and environment settings." Confidence: 0.60
- Reports that a delivered implementation is not working with a terse, content-free status message (e.g., "not working da") and no further detail — expects the assistant to investigate autonomously (pull logs, query service/DB state, inspect asset-event history, trace timing/races between DAGs) until it finds the real root cause and fixes it, rather than asking clarifying questions or re-verifying what they mean. Confidence: 0.50
- Each DAG's working directories (download/temp dir, extracted-images output dir) must be declared in that DAG's own `config.yml` and resolved to absolute paths at runtime — the direct-indexing DAG previously borrowed `download_dir` from the filenest config and hardcoded `storage/images`; the user asked to add "temp/ images also" and the fix added `indexing.temp_dir` (`aiplatform/storage/temp`) and `indexing.images_dir` (later moved to the temporary `aiplatform/storage/temp/images` because extracted PDF/DICOM images are disposable artifacts, not persistent storage) to the indexing config.yml, with the DAG reading them via `_abs_path` (fallback default retained) so extractor configs (`output_image_dir`) and the downloader point at the configured dirs instead of borrowed/hardcoded paths. Confidence: 0.65
- External/upstream APIs return identifiers as native types (e.g., fhir-staging returns `patient_id` as an int `10001`), but typed Pydantic metadata models declare them as strings — when building metadata from external event/record data, identifiers (`patient_id`, `file_id`, `encounter_id`, `service_request_id`) must be coerced to strings (`str(value)`) with `None` preserved rather than letting pydantic reject the int with a ValidationError. Fresh evidence: the direct-indexer run died at `ChunkMetadata.patient_id` because the staging API's int patient_id hit a `str | None` field; the fix was a `_to_str()` coercion helper in the chunker. Confidence: 0.55
- After building a new component/layer, the user expects a dependency-and-docs pass over it — "go through the document indexing codebase and add into needed dependencies, add into uv, readme like that da": audit the new code's imports, add any missing packages (e.g., `opensearch-py` for the indexing layer) to BOTH `pyproject.toml` (`[project]` deps so `uv sync` picks it up) AND `requirements.txt`, then surface the layer in `README.md` (component-layer table row, Airflow DAG table rows, a dedicated usage section) and `example.env` (new env vars). Treat `opensearch-py` as the PyPI name for the `opensearchpy` module, and verify the imports/tests still pass after the dependency pass. Confidence: 0.60
dded `indexing.temp_dir` (`aiplatform/storage/temp`) and `indexing.images_dir` (`aiplatform/storage/images`) to the indexing config.yml, with the DAG reading them via `_abs_path` (fallback default retained) so extractor configs (`output_image_dir`) and the downloader point at the configured dirs instead of borrowed/hardcoded paths. Confidence: 0.65
- When asked whether their codebase is clean/good and for feedback ("can u clarifyy my codebase is clean and good or any feedback for me"), wants an honest, grounded code review rather than generic reassurance — the assistant verifies specific issues in the actual code first (grep/glob for broken factory signatures, dead/unmigrated test imports, committed `.bak` files, stray `print()` calls), leads with what is genuinely good, then enumerates real problems in priority order (broken code paths, dead tests, repo hygiene), and closes with a concrete prioritized cleanup plan offering to do the small safe fixes first. Confidence: 0.65
- Airflow 3 asset-triggered tasks must declare `inlets=[asset]` on the PythonOperator itself (not just `schedule=[asset]` on the DAG) — `InletEventsAccessors` is built from the task's `inlets`, so without it `context["inlet_events"]` is empty even though the DAG is asset-scheduled, and reading the producer's `outlet_events` metadata silently yields nothing ("No asset event metadata received"). Consuming event metadata requires both the schedule-level subscription and the task-level `inlets` declaration. Confidence: 0.75
- When a producer emits an Airflow Asset on a fixed schedule (e.g., every minute), the outlet event fires on EVERY successful task run even when there is no work — so downstream asset-triggered DAGs must tolerate empty events (no per-file metadata) by logging and returning/skipping gracefully rather than raising, and should only act on events that carry a usable identifier (e.g., a non-empty `file_id`) instead of hard-failing on the bare trigger. Confidence: 0.70
- External/upstream record data may contain malformed identifiers (leading whitespace, literal label prefixes like "File ID: ..."), so orchestration should defensively sanitize/trim identifiers at the boundary (`strip()`, strip a "File ID:"/"file id:" prefix) before using them for lookups/downloads — while also flagging that the real fix belongs upstream in the writing service, not papering over it silently. Confidence: 0.55
